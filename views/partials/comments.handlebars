<div class="comments">
    {{#each this}}
    <section class="comment">
        <div>
            By <strong>{{user.username}}</strong>. {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>